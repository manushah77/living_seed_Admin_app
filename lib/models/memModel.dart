class SelectedMem {
  String? id;
  String? name;
  String? earningBalance;
  bool? isSelected;

  SelectedMem(
      {required this.earningBalance,
      this.isSelected,
      required this.id,
      required this.name});
}
